cmake_minimum_required(VERSION 3.0.2)
project(ros_to_rosbridge)
find_package(catkin REQUIRED COMPONENTS rospy)
catkin_package()
include_directories(${catkin_INCLUDE_DIRS})
add_custom_target(${PROJECT_NAME}_install_python_package ALL COMMAND python -m pip install roslibpy)
